# JSON 解析与重试 (叹息之墙)
pydantic>=2.0.0

# 铁血 SSH 执行器
paramiko>=3.0.0

# 工业级 SSH 封装 (Fabric)
fabric>=3.0.0

# 模型路由与 API 请求
requests>=2.31.0
python-dotenv>=1.0.0

# Anthropic SDK (MiniMax API 兼容)
anthropic>=0.25.0

# FastAPI Web 服务
fastapi>=0.110.0
uvicorn>=0.27.0

# LLMOps 监控 (Arize Phoenix)
arize-phoenix>=1.0.0
openinference-instrumentation-anthropic>=1.0.0

# 向量检索引擎 (ChromaDB RAG)
chromadb>=0.4.0
sentence-transformers>=2.2.0
